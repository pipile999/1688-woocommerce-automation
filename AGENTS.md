每次进入项目先读取根目录 SITE.md，站点身份与生产入口以它为准。禁止跨站读取凭证、缓存或业务规则。

# 项目共同入口

每次开始本项目任务，先读取根目录 `PROJECT_CONTEXT.md`，再按当前任务分区读取其中引用的现有规则。不要仅凭本任务聊天记录推断网站、业务或历史完成状态。

- 共用同一个项目目录，不建立分区独立代码副本或独立配置。
- 当前任务分区由任务初始化说明或任务标题确定：自动上架 / SEO优化 / 广告计划。已有「自动上架」保留其职责；用户明确限定的本次范围优先。
- 短指令结合共享上下文、分区职责及最近明确商品范围理解；无法唯一定位商品时只询问必要信息，不猜测产品或扩大范围。
- 不查看、打印、记忆或泄露密码、Token及密钥；凭证只由既有程序通过本地环境安全使用。禁止提交凭证。
- 文件存于本项目 D:\codex 目录下，命名清晰；编码前思考，简洁优化，精准修改，目标驱动执行。
- 共享文件不是跨任务完整聊天记忆。历史事实按 `PROJECT_CONTEXT.md` 的索引读取既有结果、checkpoint和audit；不要批量重读全部记录。
- 分区建立/初始化仅检查共享上下文可读，不触发业务流程、网站写入、SEO执行或广告投放。
